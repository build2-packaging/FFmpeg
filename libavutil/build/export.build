$out_root/
{
  include src/
}

lgpl_only = $($out_root/ config.libavutil.lgpl_only)

if (!$null($lgpl_only) && $lgpl_only)
  assert ($import.target != liba{avutil}) "avutil may not be imported statically, LGPL compliance: config.libavutil.lgpl_only=$lgpl_only"

export $out_root/src/$import.target
