$out_root/
{
  include src/
}

lgpl_only = $($out_root/ config.libswresample.lgpl_only)

if (!$null($lgpl_only) && $lgpl_only)
  assert ($import.target != liba{swresample}) "swresample may not be imported statically, LGPL compliance: config.libswresample.lgpl_only=$lgpl_only"

export $out_root/src/$import.target
