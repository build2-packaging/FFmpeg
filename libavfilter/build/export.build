$out_root/
{
  include src/
}

lgpl_only = $($out_root/ config.libavfilter.lgpl_only)

if (!$null($lgpl_only) && $lgpl_only)
  assert ($import.target != liba{avfilter}) "avfilter may not be imported statically, LGPL compliance: config.libavfilter.lgpl_only=$lgpl_only"

export $out_root/src/$import.target
