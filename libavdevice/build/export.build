$out_root/
{
  include src/
}

lgpl_only = $($out_root/ config.libavdevice.lgpl_only)

if (!$null($lgpl_only) && $lgpl_only)
  assert ($import.target != liba{avdevice}) "avdevice may not be imported statically, LGPL compliance: config.libavdevice.lgpl_only=$lgpl_only"

export $out_root/src/$import.target
