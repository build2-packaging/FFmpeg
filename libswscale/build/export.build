$out_root/
{
  include src/
}

lgpl_only = $($out_root/ config.libswscale.lgpl_only)

if (!$null($lgpl_only) && $lgpl_only)
  assert ($import.target != liba{swscale}) "swscale may not be imported statically, LGPL compliance: config.libswscale.lgpl_only=$lgpl_only"

export $out_root/src/$import.target
