$out_root/
{
  include src/
}

lgpl_only = $($out_root/ config.libpostproc.lgpl_only)

if (!$null($lgpl_only) && $lgpl_only)
  assert ($import.target != liba{postproc}) "postproc may not be imported statically, LGPL compliance: config.libpostproc.lgpl_only=$lgpl_only"

export $out_root/src/$import.target
